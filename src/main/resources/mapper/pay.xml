<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.greenstock.repository.interfaces.PayRepository">
 
	<insert id="insertPayInfo">
	  	insert into pay_tb(aid, tid, cid, sid, amountTotal, created_at, approved_at, user_id)
	  	values(#{aid}, #{tid}, #{cid}, #{sid}, #{amountTotal}, #{created_at}, #{approved_at}, #{userId})
  	</insert>
  	
  	<update id="modifyPayInfo">
		update pay_tb set amount_total = #{amountTotal} where id = #{id}
	</update>
  	
</mapper>