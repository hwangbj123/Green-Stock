<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.greenstock.repository.interfaces.ChatRepository"> 

	<insert id="createChatRoom">
		insert into chatroom(room_number, room_name)
		values(#{roomNumber}, #{roomName})
	</insert>
	
	<select id="findChatRoomById" resultType="com.green.greenstock.repository.model.ChattingRoom">
		select * from chatroom
		where room_id=#{roomId}
	</select>
</mapper>